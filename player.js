class Player{
    constructor(x = 20, y = 100, width = 50, height = 50, color = "green", ctx){
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
        this.color = color;
        this.ctx = ctx;
    }

    jump(){
        // resta a this.y = this.y - 3;
    }
}